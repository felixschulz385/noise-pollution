"""The reviewer-side barrier-audit app. It ships in a zip to a laptop that
has only Anaconda's base Python, so this package uses the standard library
alone (a test enforces that). See docs/data/sweden/barrier_audit/README.md.

Run from the unzipped folder: ``python -m barrier_audit``.
"""

APP_VERSION = "0.3.0"
MAPLIBRE_VERSION = "5.24.0"
