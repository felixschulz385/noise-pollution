"""Manual side audit of noise barriers against Lantmäteriet aerial imagery.
`app/` is the reviewer-side app (standard library only, shipped as a zip);
the modules here build its packages. See docs/data/sweden/barrier_audit/README.md."""
