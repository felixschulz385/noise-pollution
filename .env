# Environment variables only — no configuration values here.
# Lets `python -m src.cli` and `from src.… import …` resolve when running from
# the repository root without relying on the editable install.
PYTHONPATH=.
